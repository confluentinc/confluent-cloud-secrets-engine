name: pie-cc-hashicorp-vault-plugin
lang: go
lang_version: 1.19.0
git:
  enable: true
github:
  enable: true
semaphore:
  enable: true
make:
  enable: true
  update_makefile: false
  enable_updates: true
